# Copy to .env — REQUIRED. This example runs a real, instrumented agent and has
# no offline mock; without a key it only renders a "set the key" message.
ANTHROPIC_API_KEY=sk-ant-...

# Optional — defaults to 3003
# PORT=3003
